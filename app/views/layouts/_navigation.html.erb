<nav class="top-bar" data-topbar role="navigation">
  <!-- Left Nav Section -->
  <ul class="title-area">
    <li class="name">
      <h1><%= link_to 'Family Tree Book', root_path %></h1>
    </li>
  </ul>
  <section class="top-bar-section">
    <!-- Right Nav Section -->
    <ul class="right">
      <li>
        <%= link_to 'Home', root_path %>
      </li>
      <li class="divider"></li>
      <% if user_signed_in?  %>
        <li class="has-dropdown">
          <%= link_to current_user.full_name %>
          <ul class="dropdown">
            <li><%= link_to 'Update Profile', edit_user_registration_path %></li>
            <li><%= link_to 'Sign out', destroy_user_session_path,method: :delete %></li>
          </ul>
        </li>
        <li class="divider"></li>
          <li class="has-form">
            <%= link_to 'Update my status', new_status_path, class: "small button" %>
        </li>
      <% else %>
        <li>
          <%= link_to 'Sign up', new_user_registration_path %>
        </li>
        <li class="divider"></li>
        <li>
          <%= link_to 'Sign in', new_user_session_path %>
        </li>            
        <li class="divider"></li>
      <% end %>
    </ul>
  </section>
</nav>

